export let API_URL = 'https://api.livinginhotel.com/api' // 개발

const v1Url = `${API_URL}/v1`
const v2Url = `${API_URL}/v2`

const mainBaseUrl = `${v2Url}/main` // 홈

export let main = {
  MAIN: `${mainBaseUrl}`
}
